{
  "name": "pocket-option-js-bot",
  "version": "1.4.0",
  "description": "Pure JS AI Trading Bot with Human Mimicry",
  "main": "server.js",
  "scripts": {
    "setup": "npm install playwright dotenv && npx playwright install chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "playwright": "^1.40.0"
  }
}
