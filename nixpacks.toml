[phases.setup]
nixPkgs = ["...", "nodejs", "python3"] # Keeps your existing env

[phases.build]
cmds = [
    "npm install",
    "npx playwright install --with-deps chromium"
]

[start]
cmd = "node server.js"
