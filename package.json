{
  "name": "pocket-option-ai-bot",
  "version": "1.3.0",
  "scripts": {
    "setup": "npm install dotenv && pip3 install requests playwright numpy && playwright install chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "playwright": "^1.40.0"
  }
}
